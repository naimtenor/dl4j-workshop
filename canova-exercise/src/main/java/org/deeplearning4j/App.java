package org.deeplearning4j;

import java.io.IOException;

/**
 * entry point for canova exercise<br>
 *
 * @author naimtenor
 *
 */
public class App 
{
    public static void main( String[] args ) throws IOException {
        // bmp to jpg
        /*File input = new File("/home/ubuntu/dl4j/data/mlsp_contest_dataset/supplemental_data/filtered_spectrograms");

        if(input.isDirectory()) {
           for(File f : input.listFiles()) {
               System.out.println(f.getName());
               BufferedImage image = ImageIO.read(f);
               if (f.getName().endsWith(".bmp")) {
                   File of = new File("/home/ubuntu/dl4j/data/mlsp_contest_dataset/supplemental_data/filtered_spectrograms_jpg/" + f.getName().substring(0, f.getName().lastIndexOf(".")) + ".jpg");
                   ImageIO.write(image, "jpg", of);
               }

           }
        }*/
    }
}
